name: CI/CD — SwingTrader Platform

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  # ─────────────────────────────────────────────
  # BUILD BACKEND
  # ─────────────────────────────────────────────
  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install deps
        run: npm install --legacy-peer-deps
        working-directory: apps/backend-nestjs

      - name: Generate Prisma client
        run: npx prisma generate
        working-directory: apps/backend-nestjs

      - name: Build
        run: npm run build
        working-directory: apps/backend-nestjs

      - name: Install production deps only
        run: npm install --only=production --legacy-peer-deps
        working-directory: apps/backend-nestjs

      # Upload the entire backend folder (dist + node_modules + prisma) as one artifact
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-dist
          path: apps/backend-nestjs
          retention-days: 1

  # ─────────────────────────────────────────────
  # DEPLOY BACKEND → AZURE APP SERVICE
  # ─────────────────────────────────────────────
  deploy-backend:
    name: Deploy Backend to Azure
    runs-on: ubuntu-latest
    needs: build-backend
    if: github.ref == 'refs/heads/main'
    environment: production
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-dist
          path: backend-deploy

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_BACKEND_APP_NAME }}
          package: backend-deploy

  # Frontend is deployed via SWA CLI locally (see README)
