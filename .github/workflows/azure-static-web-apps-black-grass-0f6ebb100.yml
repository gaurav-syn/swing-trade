name: Azure Static Web Apps CI/CD (disabled — deploy manually via SWA CLI)

on:
  workflow_dispatch:

jobs:
  # Disabled: frontend is deployed manually using SWA CLI
  # swa deploy apps/frontend-nextjs/out --deployment-token "TOKEN" --env production
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
